extends /layouts/app.pug

block content
  .container
    .row.pt-4
      .col-4.mx-auto
        h5.text-center.pb-3 Delete account
        +formFor(formElement, { url: urlFor('settings'), method: 'DELETE' })
          +input(formElement, 'password', 'Password', { as: 'password' })
          +button('danger', 'Delete')
